---
date: 2026-06-03
---

# 2026-06-03 工作日志

---

## 任务索引

| # | 任务 | 模块 | 优先级 | 状态 | 关联 |
|---|------|------|--------|------|------|
| 1 | doc-check 新增 --pre-push 阻塞模式 + pre-push hook 统一检查 | tooling | **高** | ✅ 已完成 | [详情](#1-doc-check-pre-push-阻塞模式) |
| 2 | .githooks/pre-commit 接入 privacy-check | tooling | 中 | ✅ 已完成 | — |
| 3 | 项目系统审计 + 文档/代码/配置一致性修复 | quality | **高** | ✅ 已完成 | [详情](#3-项目审计与一致性修复) |
| 4 | RSSHub 配置独立化（rsshub_url + route 字段） | config | **高** | ✅ 已完成 | [详情](#4-rsshub-配置独立化) |

---

## 完成记录

### 1 doc-check pre-push 阻塞模式

**问题**：doc-check 只在 commit 时做提醒（exit 0），push 时无检查。代码改了但文档没同步的情况可以轻松推上去。

**方案**：
- `doc-check.py` 新增 `--pre-push` 模式：用 `git diff origin/main...HEAD` 获取待推送文件
- `--pre-push` 模式下发现文档未更新时 exit 1 阻塞 push，终端红色高亮
- `.githooks/pre-push` 统一执行 journal-check + doc-check --pre-push，任一失败即阻塞

---

### 3 项目审计与一致性修复

**问题**：代码规范、文档一致性、配置同步从未系统性审查。

**审计发现**：
- 代码：1 个中文注释、6 个缺失类型注解、collect.py 超 400 行、9 个超 40 行函数
- 文档：版本表停在 v2.11、组件表缺 cache.py/redis_client.py、MCP 参数简写不准、GITHUB_TOKEN 未文档化
- 配置：.scout.yml 缺 6 个 example 中的 key

**修复**（3 个 agent 并行）：
- `00-overview.md`：版本表补 v2.12、组件表补 cache/redis_client、子设计索引补 D-07
- `06-mcp.md`：工具参数修正、CLI 补 collect、key files 描述修正
- `cli.py` 5 个函数补 `-> None`、`_auth.py` dispatch 补 `-> Response` + 注释修正
- `.scout.example.yml` 补 GITHUB_TOKEN 文档和 Pydantic env prefix 说明

---

### 4 RSSHub 配置独立化

**问题**：RSSHub 识别靠硬编码 `:1200` 端口字符串，access key 注入依赖这个 hack，切换 RSSHub 实例需逐条改 URL。

**方案**：
- `config.py` 新增 `rsshub_url` 字段
- `collect.py` 删除 `_is_rsshub_url()`，新增 `_resolve_source_url()`：有 `route` 则拼接 `rsshub_url` + 注入 key，有 `url` 则直连
- 5 个 RSSHub 源从完整 URL 改为 `route` 格式（如 `/36kr/newsflashes`）
- 配置文件公开源在上、RSSHub 源在下，注释分隔

---

## 关联

- [Doc Check](../scripts/doc-check.py)
- [Journal Check](../scripts/journal-check.py)
- [Pre-push Hook](../.githooks/pre-push)
- [Config](../src/linglong/config.py)
- [Collect](../src/linglong/scout/collect.py)
