# 2026-07-03 工作日志

> 主题:早报截断/空回事故排查 + 修复(glm-5.1 思考模型)

## 任务索引

| # | 任务 | 状态 |
|---|------|------|
| 1 | 排查 7-3 早报截断 | 完成 |
| 2 | 立即修复尝试:llm_timeout 120→300 | 完成(未解决) |
| 3 | 治本尝试:减 LLM 输入(每源 top15 + 摘要 120) | 完成(未解决) |
| 4 | 真正根因 + 修复:glm-5.1 思考模型,disable_thinking | 完成 |

## 排查链

**问题**:2026-07-03 早报(/daily/ai/2026-07-03.html)在"融资动态 → Wayve"处截断,缺政策/开源/Top5;`brief --force` 重试又空回。

**误判 1(timeout)**:LLM 110–206s 返回,疑超时。改 `llm_timeout` 120→300(顺带补 6-12 漏同步)。未解决(206s 仍空)。

**误判 2(输入大小)**:实测 prompt 13 万字(399 RSS × 均 315 字)。改 `agent.py` 每源 top15(`_cap_per_source`)+ 摘要 200→120,399→247、prompt 降到 ~6 万。未解决(cap 后仍空)。

**真正根因(思考模型)**:简单 prompt 测试暴露——`max_tokens=100` 时 `reasoning_tokens=100`、content 空、finish=length;`max_tokens=4000` 时 reasoning 182 + content 完整。**glm-5.1 是思考模型,max_tokens 被 reasoning 占满,content 余量不足 → 早报截断/空**。和输入大小、端点(coding vs paas)都无关。

**修复**:`config.py` 加 `llm_disable_thinking` 开关;`_build_openai_request` body 条件加 `thinking={type:disabled}`(已测有效:reasoning_tokens=0,content 完整);`.scout.yml`/`.scout.example.yml` 启用。159 测试过。

## 结论

根因是思考模型 max_tokens 分配(thinking 占预算),非输入大小/端点。cap + timeout 改动仍有价值(prompt 控制 + 余量),但 `disable_thinking` 是关键。push 部署后 `brief --force` 验证。

## 关联

- memory `scout-blog-render`(渲染器)
