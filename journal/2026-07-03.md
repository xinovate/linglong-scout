# 2026-07-03 工作日志

> 主题:早报截断事故排查 + 修复(LLM 大输入退化)

## 任务索引

| # | 任务 | 状态 |
|---|------|------|
| 1 | 排查 7-3 早报截断 | 完成 |
| 2 | 立即修复:llm_timeout 120→300 | 完成 |
| 3 | 治本:减 LLM 输入(每源 top15 + 摘要 120) | 完成 |

## 1. 早报截断排查

**问题**:2026-07-03 早报(/daily/ai/2026-07-03.html)在"融资动态 → Wayve"处截断,缺政策/开源/Top5。

**排查**:
- raw md 6336 字节在 Wayve 处突断,Redis brief 同长 → 生成时就断,非渲染问题
- 日志:LLM 110s 返回 200 OK 但输出仅 4653 字符(截断);重试 200s 返回 0 字符(空)
- 不是 timeout(110s < 120s)、不是 max_tokens(4653 字符 << 8000 token)
- 根因:**LLM(GLM coding 接口)在 412 items 大输入下输出退化**(截断/空回随机)
- 实测 prompt:rss_text 12.6 万字(399 条 × 均 315 字)+ 模板 ≈ 13.3 万字(6-7 万 token)

## 2. 立即修复:llm_timeout 120→300

journal 2026-06-12 记过"timeout 120→300"但 `.scout.yml` 实际还是 120(没同步)。补改本地 + 服务器 + restart。但 timeout 不是主因(LLM 早停/空回非超时),治本必须减输入。

## 3. 治本:减 LLM 输入

`agent.py` 两道收紧:
- `_cap_per_source`:每源 top 15(保源覆盖砍同质冗余,feedparser 最新优先),399 → 247 条
- `_format_rss` 摘要 `[:200]` → `[:120]`
- prompt 13 万 → ~6 万字(GLM 安全区)
- 补 `_cap_per_source` 单测,159 测试全过

docs:`02-agent-pipeline.md` 加「Prompt 大小控制」节 + timeout 120→300 同步。

## 结论

减输入是治本(timeout 不是根因)。push 部署后 `brief --force` 验证。

## 关联

- memory `scout-blog-render`(渲染器)
- 治本待办记录于 personal wiki
