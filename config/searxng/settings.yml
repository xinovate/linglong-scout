# SearXNG settings — minimal config for Linglong Scout
# See https://docs.searxng.org/admin/settings/ for full options

use_default_settings: true

server:
  secret_key: "linglong-scout-dev-key"
  limiter: false

search:
  formats:
    - html
    - json
